# bkg-tools

**Ledger inspection tools. Read-only queries over the event ledger.**

`ledger_summary` provides human-readable snapshots of ledger contents.

## Tools

- `ledger_summary` — count events by type, show chain tip, detect gaps
